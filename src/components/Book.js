import React from 'react'
import Proptypes from 'prop-types';

const Book = ({book}) => {
    const { id, title, category } = book;
    return (
        <div>
        <h1>books</h1>
        </div>
    )
}

export default Book;